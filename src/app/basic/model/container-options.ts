import { TopbarOptions } from './topbar-options';

/**
 * The configurable options for the witched container
 */
export interface ContainerOptions {
  /**
   * The top bar options
   */
  topbar?: TopbarOptions;
}
